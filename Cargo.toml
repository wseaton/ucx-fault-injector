[package]
name = "ucx-fault-injector"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
once_cell = "1.19"
nix = { version = "0.29", features = ["signal"] }
libloading = "0.8"
ctor = "0.2"

[build-dependencies]
bindgen = "0.70"
